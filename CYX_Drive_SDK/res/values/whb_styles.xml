<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="AppBaseTheme" parent="android:Theme.Light"></style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- 筛选优惠券弹出框 -->
    <style name="ScreeningDialog" parent="@android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="PopupWindowAnimation">

        <!-- 优惠券分类下拉菜单视图的效果 -->
        <item name="android:windowEnterAnimation">@drawable/whb_menupopshow_anim</item>
        <item name="android:windowExitAnimation">@drawable/whb_menupophidden_anim</item>
    </style>

    <style name="MyCheckBox" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/whb_checkbox_selector</item>
        <item name="android:paddingLeft">10dip</item>
        <item name="android:minHeight">10.0dip</item>
        <item name="android:minWidth">10.0dip</item>
    </style>
    <!-- 自定义loading dialog -->  
    <style name="whb_loading_dialog" parent="android:style/Theme.Dialog">  
        <item name="android:windowFrame">@null</item>  
        <item name="android:windowNoTitle">true</item>   
        <item name="android:windowBackground">@drawable/whb_loading_bg</item>  
        <item name="android:windowIsFloating">true</item>  
        <item name="android:windowContentOverlay">@null</item>  
    </style>  
</resources>